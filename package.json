{
  "name": "react-parse-htmlstring",
  "version": "1.2.0",
  "description": "Parse any string and return a React component on a html container of your choice!",
  "main": "lib/index.js",
  "scripts": {
    "clean": "rimraf dist lib node_modules demo/dist demo/node_modules",
    "build": "npm install && npm run build:lib && npm run build:umd && cd demo && npm install && npm run build && cd ..",
    "build:lib": "rimraf lib && babel ./src -d lib",
    "build:umd": "webpack --config webpack.config.development.js",
    "prerelease": "npm run clean",
    "release": "npm run build",
    "postrelease": "git push --follow-tags && npm publish",
    "release:patch": "npm version patch && npm run release",
    "release:minor": "npm version minor && npm run release",
    "release:major": "npm version major && npm run release",
    "demo:dev": "cd demo && node ./server.js",
    "demo:clean": "rimraf demo/dist demo/node_modules",
    "demo:build": "cd demo && npm install && npm run build",
    "demo:push": "cd demo/dist && git init && git commit --allow-empty -m 'update demo' && git checkout -b gh-pages && git add . && git commit -am 'update demo' && git push git@github.com:jaosimt/react-parse-htmlstring gh-pages --force",
    "demo:publish": "npm run demo:clean && npm run demo:build && npm run demo:push"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jaosimt/react-parse-htmlstring.git"
  },
  "keywords": [
    "react",
    "parse",
    "html",
    "string",
    "htmlString",
    "htmlstring",
    "html-string"
  ],
  "author": "Sim Jao",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.21.0",
    "@babel/core": "^7.13.10",
    "@babel/preset-env": "^7.13.10",
    "@babel/preset-react": "^7.12.13",
    "babel-loader": "^8.2.2",
    "eslint": "^8.36.0",
    "eslint-plugin-react": "^5.1.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "rimraf": "^2.5.2",
    "webpack": "^5.27.0",
    "webpack-cli": "^4.5.0"
  },
  "dependencies": {
    "classnames": "^2.3.2"
  },
  "peerDependencies": {
    "react": "^15.0.0 || ^16.0.0-0 || ^17.0.0"
  }
}
